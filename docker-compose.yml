version : 3.9

services:
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    valumes:
      - . : /code
    ports: 
      - 8000:8000
  
  db:
    image: postgres:15
    environment:
      - "POSTGRES_HOST_AUTH_METHOD=trust"
